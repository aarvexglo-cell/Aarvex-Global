{
  "name": "claude-curator",
  "version": "0.1.0",
  "description": "A small webhook service that validates SVG assets and uses Claude API to suggest and apply fixes via GitHub PRs.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "optimize:icons": "svgo \"..\\Icons\\*.svg\" --multipass"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2",
    "simple-git": "^3.19.0"
  }
}